<Window x:Class="StarLibEditor.Views.Dialogs.TemplateManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Template Manager" Width="600" Height="450"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Template list -->
        <DockPanel Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Text="TEMPLATES" Foreground="#888" FontSize="11"
                       FontWeight="Bold" Margin="0,0,0,8"/>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Import..." Click="Import_Click" Padding="8,3"
                        Background="#333" Foreground="White" BorderThickness="0" Margin="0,0,4,0"/>
                <Button Content="Export..." Click="Export_Click" Padding="8,3"
                        Background="#333" Foreground="White" BorderThickness="0"/>
            </StackPanel>
            <ListBox x:Name="TemplateList" Background="Transparent" BorderThickness="0"
                     Foreground="White" SelectionChanged="TemplateList_SelectionChanged"/>
        </DockPanel>

        <!-- Template details -->
        <DockPanel Grid.Column="1" Margin="12,0,0,0">
            <TextBlock DockPanel.Dock="Top" Text="TEMPLATE DETAILS" Foreground="#888" FontSize="11"
                       FontWeight="Bold" Margin="0,0,0,8"/>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Delete" Click="Delete_Click" Padding="12,4"
                        Background="#C0392B" Foreground="White" BorderThickness="0" Margin="0,0,8,0"/>
                <Button Content="Insert to Tab" Click="InsertToTab_Click" Padding="12,4"
                        Background="#0078D4" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
            </StackPanel>
            <StackPanel x:Name="DetailsPanel"/>
        </DockPanel>
    </Grid>
</Window>
