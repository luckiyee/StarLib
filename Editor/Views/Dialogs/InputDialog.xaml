<Window x:Class="StarLibEditor.Views.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Width="350" Height="160"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E1E">
    <StackPanel Margin="16">
        <TextBlock x:Name="PromptText" Text="Enter value:" Foreground="#E0E0E0" FontSize="13" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" FontSize="13" Padding="6,4" Margin="0,0,0,16"
                 Background="#2D2D2D" Foreground="White" BorderBrush="#555" BorderThickness="1"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Padding="16,4" Margin="0,0,8,0"
                    Background="#333" Foreground="White" BorderThickness="0"/>
            <Button Content="OK" Click="OK_Click" Padding="16,4" IsDefault="True"
                    Background="#0078D4" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
        </StackPanel>
    </StackPanel>
</Window>
