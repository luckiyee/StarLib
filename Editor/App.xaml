<Application x:Class="StarLibEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StarLibEditor"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
            <local:ColorToBrushConverter x:Key="ColorToBrush"/>
            <local:InverseBoolConverter x:Key="InverseBool"/>
            <SolidColorBrush x:Key="PanelBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="PanelBorder" Color="#333333"/>
            <SolidColorBrush x:Key="ToolbarBackground" Color="#252525"/>
            <SolidColorBrush x:Key="CanvasBackground" Color="#121212"/>
            <SolidColorBrush x:Key="TextForeground" Color="#E0E0E0"/>
            <SolidColorBrush x:Key="TextMuted" Color="#888888"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#00AAFF"/>
            <SolidColorBrush x:Key="AccentHover" Color="#33BBFF"/>
            <SolidColorBrush x:Key="SelectedBrush" Color="#0078D4"/>
            <SolidColorBrush x:Key="InputBackground" Color="#2D2D2D"/>
            <SolidColorBrush x:Key="SplitterBrush" Color="#404040"/>

            <Style TargetType="GridSplitter">
                <Setter Property="Background" Value="{StaticResource SplitterBrush}"/>
                <Setter Property="ShowsPreview" Value="True"/>
            </Style>

            <Style x:Key="PanelHeader" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextForeground}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Margin" Value="8,6"/>
            </Style>

            <Style x:Key="ToolbarButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextForeground}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#3A3A3A"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="MenuItemStyle" TargetType="MenuItem">
                <Setter Property="Foreground" Value="{StaticResource TextForeground}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
